# Annotation policy

Allowed annotations are `TODO`, `FIXME`, `CHORE`, `PERF`, `SECURITY`, `COMPAT`, and `DOCS`. Each annotation must include an issue reference such as `Issue: Forebase/Eventful#123`. Run `python scripts/check_annotations.py` before opening a PR.
