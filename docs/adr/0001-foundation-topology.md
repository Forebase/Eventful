# ADR 0001: Foundation topology

## Decision

Create the expected v1 namespaces in 0.2 with contracts and explicit stability markers before adding production integrations.

## Consequences

Future 0.x releases can fill existing namespaces instead of introducing new top-level packages.
